# VITE_GEMINI_API_KEY: Optional. If set, the static site bakes this key
# into the build and chat works without asking visitors for a key.
# In Vercel, add this under Project Settings -> Environment Variables.
VITE_GEMINI_API_KEY=MY_GEMINI_API_KEY
